# DESIGN.md — 月刊OGYANUN / 見た目の契約

> このファイルは**AIエージェントが最初に読む見た目の契約書**。
> Vault側の正本は `MyBrain/Templates/DESIGN.md_家の設計言語.md`（資料用の「号1」もそこにある）。
>
> **値の正本は `src/styles/global.css`。** 下のスナップショットと食い違ったら **CSSが勝つ。**
> 差分は `npm run check:design` が機械で見張っている（このファイルを手で直したら必ず回す）。

コンセプト（本人の言葉）：**遠目はファッション誌70% / 近づくとコロコロ30%**

---

## 1. 色

**ページ側のCSSへ色をベタ書きしない。** 新しい色は `global.css` のトークンへ足す。
ベタ書きすると夜にしたとき**そこだけ昼のまま取り残される**（実際 `#ddd` / `#e2e2e2` / `#e6e6e6` と3か所に散っていた → `--hair-rule` に統合）。

| 変数 | 役割 |
|---|---|
| `--paper` | 紙。ページの地 |
| `--paper-2` | 少しだけ紙寄りの白。表ヘッダ・コード地・敷き |
| `--ink` | 墨。本文と見出し |
| `--ink-mid` | 補足・注記・弱い文字 |
| `--rule` | 主張する罫（柱・h2の下） |
| `--hair-rule` | 主張しない細い罫（表の行・一覧の区切り） |
| `--accent` | **コロコロ側の担当。使うのはラベル・番号・矢印だけ** |
| `--accent-ink` | accent を敷いた上に乗る文字 |
| `--mag-ring` / `--mag-drop` | 中央の一冊のフチと影。ここが紙の厚みに見える |

### スナップショット（正本は global.css）

<!-- design-tokens:light -->
```css
--paper: #ffffff;
--paper-2: #f4f2ed;
--ink: #101010;
--ink-mid: #4a4a4a;
--rule: #101010;
--hair-rule: #e2e2e2;
--accent: #e0301e;
--accent-ink: #ffffff;
```

<!-- design-tokens:dark -->
```css
--paper: #121211;
--paper-2: #1d1c1a;
--ink: #f0ede6;
--ink-mid: #a29d94;
--rule: #f0ede6;
--hair-rule: #33312d;
--accent: #ff6a52;
--accent-ink: #1a1310;
```

> [!WARNING] 夜の値は**同じものを2か所に書いてある。片方だけ直さない**
> `:root[data-theme="dark"]` と `@media (prefers-color-scheme: dark)` の両方。
> `light-dark()` なら1か所で済むが、**非対応ブラウザで色が全部無効値になって読めなくなる。**
> 二重に書いておけば古い環境には昼の号がそのまま出るだけで済む。
> → この2か所のズレは `npm run check:design` が落とす。

**中央の一冊（`.mag`）は刷り色を固定してある。** 印刷された物体なので昼夜で色が動くと嘘になる。

---

## 2. 組み（タイポグラフィ）

| 役割 | 値 |
|---|---|
| 本文 | `17px` / `line-height 1.85` / `font-feature-settings: "palt" 1` |
| 本文見出し | h2 `1.35rem`（下に `--rule` の罫）／ h3 `1.1rem` |
| 柱（`.rail`） | `11px` / `letter-spacing .18em` / uppercase |
| ラベル（`.label`） | `10px` / `700` / `letter-spacing .16em` / radius `2px` |
| 番号（`.no`） | 欧文 `700` italic / `--accent` |

| 用途 | 変数 | 中身 |
|---|---|---|
| 和文 | `--font-jp` | Hiragino Kaku Gothic ProN → Yu Gothic → Noto Sans JP → system-ui |
| 明朝 | `--font-mincho` | Hiragino Mincho ProN → Yu Mincho |
| 欧文 | `--font-en` | Helvetica Neue → Helvetica → Arial |
| 誌名専用 | `--font-display` | Bodoni Moda → Didot → Bodoni MT → Times New Roman |

**日本語ウェブフォントは読み込まない。** 数MB級で決定パターン4「軽さ＝継続可能性」に反する。
題字のインパクトは**サイズ差・字間・レイアウト**で出す。
欧文だけは例外で、誌名の Bodoni Moda（ラテン部分集合・数十KB）を1本だけ。**これ以上増やさない。**

---

## 3. 版面と形

| 項目 | 変数 | 値 |
|---|---|---|
| 外余白 | `--gut` | `clamp(16px, 4vw, 56px)` |
| 罫の太さ | `--hair` | `1px` |
| ページめくり | `--turn` | `260ms`（**伸ばさない**） |
| 一冊の判型 | `--mag-ratio` | `1 / 1.32` |
| 本文の幅 | `.prose` | `40rem` |
| 版面の幅 | `.measure` | `62rem` |

- **角丸はほぼ使わない。** 例外はラベルの `2px` だけ
- 構造は**罫と余白**で出す。影・カード・グラデーションで出さない
- フォーカスリングは `--accent` の `3px` アウトライン（offset `3px`）
- 表紙以外はリンクに下線（可読性優先）
- 紙をめくる物理シミュレーションはしない。**横へ送るだけ**

---

## 4. Do

- 色は `global.css` のトークンへ足してから使う
- 夜の値は**2ブロック両方**を直す
- 見出しは言い切る。「〜について」「〜とは」を使わない
- 三点リーダーは `……`／ダッシュは `──`／年号範囲は enダッシュ `1890–1937`
- 画像は必ず webp（元のPNGは1枚2MB級だった）

## 5. Don't

角丸カードの反復 ／ 3枚横並び ／ 2×2カード ／ 影付きカード ／ 均等3カラム ／
青紫グラデーション ／ ガラス風UI ／ ネオン ／ 過剰なアイコン ／ 意味のない英語ラベル ／
全ページ同じレイアウト ／ 過度に細いフォント ／ 視覚的焦点がない

加えてこのサイト固有：

- ページ側のCSSへ色をベタ書きする
- 表紙以外に派手な演出を足す（**メリハリが死ぬ**）
- 日本語ウェブフォントを読み込む
- 巨大JSライブラリ・3Dページめくり

---

## 6. ゲーム（`public/games/*`）

**フォント指定は上の `--font-jp` / `--font-en` をそのまま持ってくる**（既存2作とも同一）。
色は CSS 変数として `:root` の先頭に置く。素のHTML/JS 1枚で完結させる（ビルド不要）。

> [!NOTE] 色は「サイトを継ぐ」か「作品の夜を作る」かで分かれている
> **標本は2件だけ。確立したルールではない。** 3件目でどちらを継ぐか判断する。

| 作品 | 紙 | 墨 | 差し色 |
|---|---|---|---|
| キキ？キサキ？ | `#ffffff` | `#101010` / mid `#6a6a6a` | `#e0301e` ＋ `#1a7f37`（正解） |
| アリアのケツで刻め！ | `#07070a` / `#141419` | `#f2f2f0` / mid `#8d8d95` | `#ffe11a`（閃光）／ `#ff3b3b`（被弾） |

---

## 7. 似ている先

自分の設計言語を説明するときの参照点。**真似する先ではない。**

| 先 | どこが近いか |
|---|---|
| 雑誌の誌面（ファッション誌） | 版面・柱・ノンブル・非対称の余白・大きな題字 |
| コロコロコミック | 赤の差し色・番号・矢印・言い切る見出し |
| Swiss / International Typographic Style | 罫と余白で構造を出す。装飾で出さない |
